import {scrollIntoView} from '../../../../src/dogma/utils/animation'

describe('dogma / utils / animation', function () {
  it('exports scrollIntoView', function () {
    expect(typeof scrollIntoView).to.equal('function')
  })
})
